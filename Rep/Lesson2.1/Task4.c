#include <stdio.h>

int main(void) 
{
	printf("Сиволы, которые недопустимо использовать при именовании: \n ");
	printf("%s","! \" # % & ' ( ) * + , - . / : ; < > = ? @ [ ] \\ ^ ` { } | ~ ");
	return 0;
}
